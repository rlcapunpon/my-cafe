require('rootpath')();
var express = require('express');
var app = express();
var cors = require('cors');
var bodyParser = require('body-parser');
var expressJwt = require('express-jwt');
var config = require('config.json');

app.use(cors());
app.use(bodyParser.urlencoded({ extended: false }));
app.use(bodyParser.json());

// use JWT auth to secure the api, the token can be passed in the authorization header or querystring
// app.use(expressJwt({
//     secret: config.secret,
//     getToken: function (req) {
//         if (req.headers.authorization && req.headers.authorization.split(' ')[0] === 'Bearer') {
//             return req.headers.authorization.split(' ')[1];
//         } else if (req.query && req.query.token) {
//             return req.query.token;
//         }
//         return null;
//     }
// }).unless({ path: paths }));

// routes
app.use('/special', require('./controllers/special.controller'));
app.use('/pasta', require('./controllers/pasta.controller'));
app.use('/colddrinks', require('./controllers/colddrinks.controller'));
app.use('/desserts', require('./controllers/desserts.controller'));
app.use('/hotbeverage', require('./controllers/hotbeverage.controller'));
app.use('/orders', require('./controllers/orders.controller'));
app.use('/login', require('./controllers/login.controller'));

// start server
var port = process.env.NODE_ENV === 'production' ? 80 : 4000;
var server = app.listen(port, function () {
    console.log('Server listening on port ' + port);
});